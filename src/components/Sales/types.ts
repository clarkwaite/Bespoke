export type SaleFormData = {
    productId: number;
    salesPersonId: number;
    customerId: number;
    date: string;
    id?: number;
};

export type DateRange = {
    startDate: string
    endDate: string
}